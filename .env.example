# PostgreSQL credentials (identical for db, Odoo via HOST/USER/PASSWORD, and webapp via PG*)
POSTGRES_USER=odoo
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=postgres

# Optional: override if your code reads these variables
ODOO_URL=http://odoo:8069
